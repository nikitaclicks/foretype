# Build output
/build/
/Foretype.app
/.build/
DerivedData/
*.xcuserstate

# Release artifacts (the cask in dist/Casks/ is committed; the zip + build
# manifest are not — they're regenerated by scripts/build.sh)
/dist/*.zip
/dist/.build-*.env

# macOS
.DS_Store

# Xcode user-specific
xcuserdata/

.claude
