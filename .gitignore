# Build output
/build/
/Foretype.app
/.build/
DerivedData/
*.xcuserstate

# Release artifacts (the cask in dist/Casks/ is committed; the zip is not)
/dist/*.zip

# macOS
.DS_Store

# Xcode user-specific
xcuserdata/

.claude
